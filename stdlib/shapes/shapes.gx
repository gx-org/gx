package shapes

import "dtype"

// DType is the datatype of an array.
type DType int32

// Shape of an array.
type Shape struct {
	DType      DType
	Dimensions []intlen
}

// Declare built-ins: these are typechecked like normal GX functions.
func Len[T dtype.Num]([___M]T) int64
