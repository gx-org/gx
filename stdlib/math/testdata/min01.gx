package min01

import "math"

func f() [2]int32 {
	return math.Min([2]int32{1, 2}, [3]int32{1, 2, 3}) // ERROR cannot use array of shape []int{2} as scalar or array of shape []int{3} (expect same shape)
}
